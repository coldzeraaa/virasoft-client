query product($slug: String!) {
  product(slug: $slug) {
    id
    name
    title
    description
    images
    master {
      id
      price
      sku
      images
    }
    variants {
      nodes {
        id
        id
        price
        sku
      }
    }
  }
}
