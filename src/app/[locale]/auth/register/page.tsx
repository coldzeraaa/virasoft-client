import AuthRegisterClient from '@/components/page-client/auth/auth-register-client';

export default function AuthRegister() {
  return (
    <div className="container flex items-center justify-center py-10">
      <AuthRegisterClient />
    </div>
  );
}
