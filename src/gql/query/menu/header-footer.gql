query headerFooter($filter: MenuFilter) {
  menus(filter: $filter) {
    nodes {
      children {
        title
        children {
          title
          images
          link
        }
      }
    }
  }
}
